<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/content_change_password"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.ingenuitymobile.edwardlynx.activities.ChangePasswordActivity"
    tools:showIn="@layout/activity_change_password">

  <ImageView
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:scaleType="centerCrop"
      android:src="@drawable/bg"/>

  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:paddingBottom="@dimen/activity_vertical_margin"
      android:paddingLeft="@dimen/activity_horizontal_margin"
      android:paddingRight="@dimen/activity_horizontal_margin"
      android:paddingTop="@dimen/activity_vertical_margin"
      android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

      <TextView
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:text="@string/change_password_bold"
          android:textColor="@color/white"
          android:textSize="12sp"/>


      <TextView
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_marginTop="12dp"
          android:text="@string/current_password"
          android:textColor="@color/white"/>

      <EditText
          android:id="@+id/edit_current_password"
          android:layout_width="match_parent"
          style="@style/edit_text"
          android:layout_marginTop="4dp"
          android:imeOptions="actionNext"
          android:inputType="textPassword"/>
      <TextView
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_marginTop="12dp"
          android:text="@string/new_password"
          android:textColor="@color/white"/>

      <EditText
          android:id="@+id/edit_new_password"
          android:layout_width="match_parent"
          style="@style/edit_text"
          android:layout_marginTop="4dp"
          android:imeOptions="actionNext"
          android:inputType="textPassword"/>

      <TextView
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_marginTop="12dp"
          android:text="@string/re_new_password"
          android:textColor="@color/white"/>

      <EditText
          android:id="@+id/edit_new_re_password"
          android:layout_width="match_parent"
          style="@style/edit_text"
          android:layout_marginTop="4dp"
          android:imeOptions="actionDone"
          android:inputType="textPassword"/>
    </LinearLayout>

    <TextView
        android:id="@+id/text_save"
        android:layout_width="match_parent"
        style="@style/orange_button"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:onClick="createAccount"
        android:text="Save"/>
  </LinearLayout>

</RelativeLayout>
